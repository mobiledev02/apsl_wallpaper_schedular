name: apsl_wallpaper_scheduler_example
description: Example app for the apsl_wallpaper_scheduler package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  apsl_wallpaper_scheduler:
    path: ../   # local package reference

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
